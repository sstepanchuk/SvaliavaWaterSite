name: 🚀 Stage CI

on:
  push:
    branches:
      - main

jobs:
  build-and-push:
    environment: stage
    runs-on: ubuntu-latest

    steps:
    - name: 🛒 Checkout repository
      uses: actions/checkout@v4

    - name: 🐳 Set up Docker Buildx
      uses: docker/setup-buildx-action@v3

    - name: 📂 Add CA cert to Ubuntu root store and restart Docker
      run: |
        # Додаємо CA сертифікат у систему
        echo "${{ secrets.DOCKER_CA_CERT }}" | base64 -d | sudo tee /usr/local/share/ca-certificates/registry-ca.crt > /dev/null
        
        # Оновлюємо root сертифікати
        sudo update-ca-certificates
        
        # Перезапускаємо Docker для застосування нового CA
        sudo systemctl restart docker
        
        echo "✅ CA сертифікат додано у root та Docker перезапущено"


    - name: 🔑 Log in to Private Registry
      uses: docker/login-action@v3
      with:
        registry: ${{ secrets.DOCKER_REGISTRY_URL }}
        username: ${{ secrets.DOCKER_REGISTRY_LOGIN }}
        password: ${{ secrets.DOCKER_REGISTRY_PASSWORD }}

    - name: 🏗 Build and Push Docker image
      uses: docker/build-push-action@v5
      with:
        context: .
        push: true
        tags: ${{ secrets.REGISTRY_URL }}/svaliava-water-site:latest

    - name: ✅ Upload completed
      run: echo "🎉 Image successfully uploaded to Private Registry!"
